<UserControl x:Class="UbisamBase.Core.SettingItems.AllSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:UbisamBase.Core.SettingItems"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <DockPanel Grid.Column="0" Margin="0,0,24,0">
            <TextBlock DockPanel.Dock="Top" Text="등록된 설정" FontWeight="SemiBold" Margin="0,0,0,10"/>
            <ListBox ItemsSource="{Binding Keys}" SelectedItem="{Binding SelectedKey, Mode=TwoWay}"/>
        </DockPanel>

        <DockPanel Grid.Column="1" Visibility="{Binding HasSelection, Converter={StaticResource BoolToVis}}">
            <Grid DockPanel.Dock="Top" Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding SelectedKey}" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                <local:SettingsToolbar Grid.Column="1"/>
            </Grid>
            <local:SettingsPropertyGridView DataContext="{Binding Grid}"/>
        </DockPanel>

        <TextBlock Grid.Column="1"
                   Text="등록된 설정이 없습니다. ISettingsService.Register로 설정 클래스를 등록하면 여기에 나타납니다."
                   Foreground="Gray"
                   TextWrapping="Wrap"
                   Visibility="{Binding HasSelection, Converter={StaticResource BoolToVis}, ConverterParameter=Invert}"/>
    </Grid>
</UserControl>
